def build(bld):
    sources = ['test_list.c']
    bld(features = 'c',
        source = sources,
        includes = [bld.env.INCLUDE_SRC, bld.env.INCLUDE_CUTEST,
                    bld.env.INCLUDE_SPECTACULAR],
        name = 'list_tests')
